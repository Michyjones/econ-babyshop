import * as React from 'react';
import { ApolloClient, DefaultOptions } from '@apollo/src/core';
import { MockedResponse } from '@apollo/src/utilities/testing/mocking/mockLink';
import { ApolloLink } from '@apollo/src/link/core';
import { Resolvers } from '@apollo/src/core';
import { ApolloCache } from '@apollo/src/cache';
export interface MockedProviderProps<TSerializedCache = {}> {
    mocks?: ReadonlyArray<MockedResponse>;
    addTypename?: boolean;
    defaultOptions?: DefaultOptions;
    cache?: ApolloCache<TSerializedCache>;
    resolvers?: Resolvers;
    childProps?: object;
    children?: any;
    link?: ApolloLink;
}
export interface MockedProviderState {
    client: ApolloClient<any>;
}
export declare class MockedProvider extends React.Component<MockedProviderProps, MockedProviderState> {
    static defaultProps: MockedProviderProps;
    constructor(props: MockedProviderProps);
    render(): JSX.Element | null;
    componentWillUnmount(): void;
}
//# sourceMappingURL=MockedProvider.d.ts.map